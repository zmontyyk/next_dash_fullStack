"use server";

import client from "../lib/dbClient";

const clientPromise = await client
console.log(clientPromise);


export const editMemberAction = async (member:any) => {
 
        console.log(clientPromise);
        console.log("called ");
        
  };